/**
 * Filter->
 * let newArray=[]
 * for(let i=0;i<array.length;i++)
 *      if(statment)
 *          newArray.push(array[i])
 * return newArray;
 * 
 * 
 * 
 * ForEach->
 * for(let i=0;i<array.length;i++)
 * 
 * 
 * Map->
 * 
 * let newArr=[]
 * for(let i=0;i<array.length;i++)
 *      newArr.push()
 * return newArr
 */